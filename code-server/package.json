{
  "name": "code-server",
  "version": "1.0.0",
  "description": "fake server for eat and drink project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json --port 3005"
  },
  "author": "Pauline BILOA",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
